<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cambio de color</title>
</head>

<body <%= style %>>
  <h1>Seleccioná un color:</h1>
  <form action="/color" method="POST">
    <select name="color">
      <option value="">Elegí un color</option>
      <option value="yellow" <% if (color == "yellow") { %> selected <% } %>>Amarillo</option>
      <option value="blue" <% if (color == "blue") { %> selected <% } %>>Azul</option>
      <option value="red" <% if (color == "red") { %> selected <% } %>>Rojo</option>
      <option value="green" <% if (color == "green") { %> selected <% } %>>Verde</option>
    </select><br>
    <label>¿Deseas recordar el color elegido?</label>
    <input type="checkbox" name="recordarColor" value="true"><br>
    <button type="submit">Elegir color</button>
  </form>
</body>

</html>