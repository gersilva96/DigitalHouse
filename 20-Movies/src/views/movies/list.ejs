<!DOCTYPE html>
<html lang="es">
<head>
    <%- include("../partials/head", {title: "Todas las películas"}) %>
</head>
<body>
    <%- include("../partials/header", {title: "Todas las películas"}) %>
    <div class="container">
        <ul>
            <% movies.forEach(movie => { %>
                <li><a href="/movies/detail/<%= movie.id %>"><%= movie.title %></a></li>
            <% }); %>
        </ul>
    </div>
    <%- include("../partials/scripts") %>
</body>
</html>